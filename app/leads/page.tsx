import {LeadsGate} from './gate';

export const metadata = {
  title: 'Leads · engineers.ge',
  description: 'CRM — პოტენციური და არსებული კლიენტები'
};

export default function LeadsPage() {
  return <LeadsGate />;
}
